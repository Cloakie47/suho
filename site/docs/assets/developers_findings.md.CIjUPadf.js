import{_ as a,o as t,c as r,a2 as s}from"./chunks/framework.7R1OEGYz.js";const p=JSON.parse('{"title":"The findings","description":"","frontmatter":{},"headers":[],"relativePath":"developers/findings.md","filePath":"developers/findings.md"}'),o={name:"developers/findings.md"};function i(n,e,l,h,c,d){return t(),r("div",null,[...e[0]||(e[0]=[s('<h1 id="the-findings" tabindex="-1">The findings <a class="header-anchor" href="#the-findings" aria-label="Permalink to &quot;The findings&quot;">​</a></h1><p>Building Suho on a young chain surfaced real behavior that the code now handles. These are the ones worth writing down.</p><h2 id="rpc-pathologies" tabindex="-1">RPC pathologies <a class="header-anchor" href="#rpc-pathologies" aria-label="Permalink to &quot;RPC pathologies&quot;">​</a></h2><p>The public RPC is load balanced. Several behaviors follow from that.</p><ul><li><strong>Stale reads.</strong> Right after a transaction, a lagging node can serve stale state, including empty code for an account that was just delegated. The fix is to read twice and trust a repeated answer, not the first one.</li><li><strong>Silent empty logs.</strong> <code>eth_getLogs</code> over a range wider than 100k blocks can return an empty array with no error. Ranges are chunked to stay under the cap.</li><li><strong>Lying backend nodes.</strong> At least one backend answers any log query with empty. Empty results are cross-checked against the Flashblocks pool, and a canary of known names refuses to serve an incomplete trust surface.</li><li><strong>Oversized multicall.</strong> A large <code>aggregate3</code> call is rejected wholesale, and <code>allowFailure</code> hides it. Batches are kept small so failures surface.</li><li><strong>Rate limits.</strong> Aggressive parallelism trips the endpoint. The fork test suite runs serially for this reason.</li></ul><h2 id="registry-traps" tabindex="-1">Registry traps <a class="header-anchor" href="#registry-traps" aria-label="Permalink to &quot;Registry traps&quot;">​</a></h2><p>The UpnameRegistry does not behave like ENS.</p><ul><li><code>ownerOf</code> returns the zero address for unregistered names instead of reverting.</li><li><code>hasActiveName(address(0))</code> returns true.</li></ul><p>Both are handled explicitly. The zero address is rejected before the active-name gate, so an unregistered name never resolves.</p><h2 id="high-s-signatures" tabindex="-1">High-s signatures <a class="header-anchor" href="#high-s-signatures" aria-label="Permalink to &quot;High-s signatures&quot;">​</a></h2><p>Real Windows Hello signatures are high-s in practice. The on-chain verifier only accepts low-s, matching the daimo malleability rule. The client normalizes each signature to low-s before submission. Without this, the classic &quot;works in the browser, reverts on chain&quot; bug appears. The account&#39;s <code>initializeWithSig</code> enforces low-s on the secp256k1 side too.</p><h2 id="the-osaka-precompile" tabindex="-1">The osaka precompile <a class="header-anchor" href="#the-osaka-precompile" aria-label="Permalink to &quot;The osaka precompile&quot;">​</a></h2><p>Fork tests run precompiles in the local EVM. The default hardfork has no P256VERIFY, but the live chain does. Fork tests set <code>evm_version = &quot;osaka&quot;</code>, which required a newer compiler binary. All pragmas stay at <code>^0.8.28</code>.</p><h2 id="the-hmr-ghost" tabindex="-1">The HMR ghost <a class="header-anchor" href="#the-hmr-ghost" aria-label="Permalink to &quot;The HMR ghost&quot;">​</a></h2><p>A long-running dev server can throw a <code>ReferenceError</code> for a constant that source no longer references. The dev bundler does not type-check, so a stale hot-reload chain can keep a phantom reference alive. Restarting the dev server clears it. Worth knowing before chasing a bug that is not in the code.</p>',15)])])}const u=a(o,[["render",i]]);export{p as __pageData,u as default};
